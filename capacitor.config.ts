import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'places-reservation-app',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
